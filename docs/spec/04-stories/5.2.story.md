---
id: STORY-5.2
epic: EPIC-5
covers_feature: CF-5
fr: FR-CF5.2
wbs_component: WBS-1.5
status: done
commits: [ade174bb8]
updated: 2026-06-08
---

# Story 5.2: 멱등 재발송 (중복 방지)

Status: done (as-built)

## Story
**운영자**는 실패 알림 재발송 시 같은 알림이 중복으로 두 번 가지 않음을 보장받는다. *(목적: 중복 알림으로 인한 혼란 방지)*

## Acceptance Criteria
1. `ReplayLedger.MarkIfNew`가 처음 본 `event_id`만 `true` 반환, 재호출 시 `false`로 재전송 방지 (FR-CF5.2)
2. process restart 후에도 멱등(파일 스캔으로 seen set 재구성)

## Tasks / Subtasks
- [x] `ReplayLedger.MarkIfNew/Has` — append-only EventID set (AC: 1)
- [x] open 시 파일 스캔으로 in-memory set 재구성, 1 MiB scanner (AC: 2)
- [x] empty EventID → `false`(저장·재전송 안 함) (AC: 1)

## Dev Notes
- 소스: `pkg/alertmanager/alertmanagernotify/dlq/ledger.go` · 커밋 `ade174bb8`
- 충족: NF-5.4.2 · 권장 idempotency 키 `(fingerprint,channel)` 확장은 open · 테스트: `dlq/ledger_test.go`
### References
- [Source: ../01-prd/features/CF-5-reliable-delivery.md#FR-CF5.2]
